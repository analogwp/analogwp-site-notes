/**
 * Vertical timeline wrapper for note comments and description.
 */
const NoteSidebarTimeline = ({ children }) => (
	<div className="sn-note-timeline">
		{children}
	</div>
);

export default NoteSidebarTimeline;
